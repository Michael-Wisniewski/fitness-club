upstream fitness_club_server {
    server fitness_club:8000 fail_timeout=5s max_fails=2; 
}

server {
    listen 80;
    listen   [::]:80 default ipv6only=on;
    
    server_name www.fitness.pl fitness.pl www.panel.fitness.pl panel.fitness.pl www.admin.fitness.pl admin.fitness.pl;
    charset     utf-8;
    client_max_body_size 10M;

    location /media  {
        alias /fitness_club/media; 
    }

    location /static {
        alias /fitness_club/static;
    }

    location / { 
        proxy_pass http://fitness_club_server;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $host;
        proxy_redirect off;
    }
}